
/*
 * Подсчитывает количество вхождений подстроки в последовательности из файла.
 *
 * @param infilename имя входного файла (C-строка)
 * @param count      ссылка на переменную для записи результата
 * @return код завершения:
 *         0 - успех,
 *        -1 - ошибка открытия файла,
 *        -2 - файл пуст,
 *        -3 - некорректные данные (пустая подстрока/последовательность,
 *              последовательность короче подстроки),
 *        -4 - повторяющиеся символы в подстроке.
 */
#pragma once
#ifndef MY_FUNCTIONS_H
#define MY_FUNCTIONS_H

int count_subsequence_in_file(const char* infilename, int& count);

#endif